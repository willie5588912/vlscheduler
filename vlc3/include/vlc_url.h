/**
 * Minimal VLC 3.x stub: vlc_url.h
 * For out-of-tree compilation only — NOT a real VLC header.
 */
#ifndef VLC_URL_H
#define VLC_URL_H

#include <vlc_common.h>

static inline char *vlc_path2uri(const char *path, const char *scheme)
{ (void)path; (void)scheme; return NULL; }

#endif /* VLC_URL_H */
